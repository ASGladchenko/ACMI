import { ToastOptions } from 'react-toastify';

export const toastConfig: ToastOptions = {
  draggable: true,
  theme: 'custom',
  autoClose: 3000,
  closeOnClick: true,
  pauseOnHover: true,
  closeButton: false,
  position: 'top-right',
};
